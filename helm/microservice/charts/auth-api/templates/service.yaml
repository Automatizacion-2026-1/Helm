# Service para Auth API
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.namespace }}-{{ .Chart.Name }}-svc
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.labels.app }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.labels.app }}