apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: santiago-wildcard
  namespace: gateway-infra
spec:
  secretName: santiago-wildcard-tls
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  dnsNames:
    - "*.santiago.monster"
    - "santiago.monster"