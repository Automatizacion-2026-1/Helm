apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: santiago-tls
  namespace: gateway-infra
spec:
  secretName: santiago-tls-secret
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  dnsNames:
    - www.santiago.monster